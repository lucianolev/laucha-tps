i= zona
j= litros
f = cantidad maxima de mosquitos que se pueden matar hasta la zona i con j litros
f(i,j)
	if i=0 { return 0 };
	res = get(i-1, j);
	for (k=1; k <=j; k++) {
		res = max(res, get(i-1, j-k) + mm(i, k));
	}
	return res;

==> comp = O(nxl^2)

get(i,j)
	if(!def(M,i,j))
		M[i,j] = f(i,j);
	return M[i,j];
	
f(n,l)

for()